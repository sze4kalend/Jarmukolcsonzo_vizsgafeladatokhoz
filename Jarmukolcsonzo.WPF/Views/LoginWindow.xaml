<Window x:Class="Jarmukolcsonzo.WPF.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Jarmukolcsonzo.WPF.Views"
        xmlns:viewmodel="clr-namespace:Jarmukolcsonzo.WPF.ViewModels"
        mc:Ignorable="d"
        Title="Bejelentkezés" Height="250" Width="320"
        d:DataContext="{d:DesignInstance Type=viewmodel:MainViewModel,IsDesignTimeCreatable=False}">
    <StackPanel Margin="20" HorizontalAlignment="Left">
        <Label Content="Bejelentkezés" FontSize="18" FontWeight="Bold"/>
        <Label Content="Felhasználónév"/>
        <TextBox Text="{Binding Username}" Width="250" TextChanged="TextBox_TextChanged"/>
        <Label Content="Jelszó"/>
        <PasswordBox PasswordChanged="PasswordBox_PasswordChanged" Width="250"/>
        <Label Content="{Binding ErrorMessage}" Foreground="red"/>
        <Button Content="Bejelentkezés" Command="{Binding LoginCommand}"/>
    </StackPanel>

</Window>
